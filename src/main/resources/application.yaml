server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jobportal_auth
    username: root
    password: "@Nikhil2025"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  application:
    name: job-service